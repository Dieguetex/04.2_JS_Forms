import { Index } from "./index.js";

/**
 * Arrancar 'npm run start.API' para que funcione. Está instalado a traves de package.json
 */


document.addEventListener('DOMContentLoaded', () => {
    new Index()
})